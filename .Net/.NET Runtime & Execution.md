1️⃣ .NET Runtime & Execution (VERY IMPORTANT)
You should deeply understand:

CLR (Common Language Runtime)

JIT compilation (when & how code is compiled)

IL code vs native code

Managed vs unmanaged code

Value types vs reference types (REAL cost difference)